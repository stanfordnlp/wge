# Default configs for email-inbox-forward-nl

explore {
  max_steps_per_episode = 6
}

policy {
  query_type = "natural-language"
}

env {
  domain = "miniwob"
  subdomain = "email-inbox-forward-nl"
}
